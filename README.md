```md
# HTTP Simple Status Checker v0.1

A short python script that performs an HTTP request to a url and shows its status (200, 403, 404), saving the results in a txt file.

This project was created for practice and learning purposes with:
- Requests
- Error management
- Writing of files
- Use of functions

## 🛠 Requirements

- Python 3.x
- Library `requests`

Install requests if you don't have:

```bash
pip install requests
